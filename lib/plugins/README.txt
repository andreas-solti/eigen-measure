The plugins here can be fetched by calling the script in the project root:

./download_dependencies.sh

They have different licenses than the eigenvalue project, which is why we cannot add them directly to the project.